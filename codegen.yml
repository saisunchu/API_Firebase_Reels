overwrite: true
schema: "https://graphqlzero.almansi.me/api" # URL to your GraphQL schema
documents: "src/screens/queries/getUser.graphql" # Path to your GraphQL operations (queries, mutations, fragments)
generates:
  src/generated/types.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
